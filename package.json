{
  "name": "umbrel-btc-powerlaw-model",
  "version": "0.1.8",
  "private": true,
  "type": "module",
  "description": "Umbrel app that maintains a continuously refit Bitcoin power-law model (price = A*t^n) from live multi-source data, with a dashboard mirroring porkopolis.io/thechart projected to 2045.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "test": "npm run build && node --test dist/*.test.js",
    "dev": "npm run build && node dist/main.js"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^22"
  }
}
